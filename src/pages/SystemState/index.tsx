export default function SystemState() {
  return (
    <div>
      This will show the selected state of the system (Bluetooth devices
      connected, Battery charging state … etc).
    </div>
  );
}
